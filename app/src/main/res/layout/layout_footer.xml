<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/head_container"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    >

    <TextView
        android:id="@+id/tv_footer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:padding="10dp"
        />

    <ProgressBar
        android:id="@+id/pb_footer"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/tv_footer"
        android:layout_toStartOf="@id/tv_footer"
        />

    <ImageView
        android:id="@+id/iv_footer"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignLeft="@+id/pb_footer"
        android:layout_alignStart="@+id/pb_footer"
        android:layout_alignTop="@+id/pb_footer"
        android:contentDescription="@null"
        />

</RelativeLayout>